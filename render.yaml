services:
  - type: web
    name: ai-business-assistant

    env: python

    plan: free

    branch: main

    buildCommand: ./build.sh

    startCommand: gunicorn AIBusinessAssistant.wsgi:application

    autoDeploy: true